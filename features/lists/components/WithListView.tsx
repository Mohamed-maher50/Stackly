// import { useAppSelector } from "@/lib/App.hooks";

// import { selectListsByBoardId } from "../listSlice";
// import ListsView from "./ListsView";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const WithListView = ({ boardId }: { boardId: string }) => {
  // const lists = useAppSelector(selectListsByBoardId(boardId));
  return <div>{/* <ListsView lists={lists} /> */}</div>;
};

export default WithListView;
